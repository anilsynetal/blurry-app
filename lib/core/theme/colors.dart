import 'package:flutter/material.dart';

// Light theme color scheme
class LightThemeColors {
  static const Color primarySet1 = Color(0xFFEB7243); // Vibrant blue
  static const Color primarySet2 = Color(0xFFA6444B); // Vibrant blue
  static const Color primarySet3 = Color(0xFF422E49); // Vibrant blue



  static const Color primary = Color(0xFFA6444B); // Vibrant blue
  static const Color primaryContainer = Color(0x1A3E97FF); // Primary with opacity for containers

  // Secondary brand colors
  static const Color secondary = Color(0xFF232222); // Teal
  static const Color buttonColor = Color(0xFF232222); // Teal
  static const Color secondaryContainer = Color(0x1A18826E); // Secondary with opacity

  // Neutral colors (for text, borders, backgrounds)
  static const Color textPrimary = Color(0xFF2E2E2E); // Primary text and icons
  static const Color clickableText = Color(0xFF0067AC); // Primary text and icons
  static const Color textSecondary = Color(0xFF4B4B4B); // Secondary text
  static const Color textSecondaryAlpha = Color(0xFF4A4A4A); // Secondary text
  static const Color textTertiary = Color(0xFF1A1A1A); // Subtle text, hints
  static const Color textDisabled = Color(0xFF6E6E6E); // Disabled or low-emphasis text
  static const Color border = Color(0x14101010); // Borders, dividers
  static const Color surfaceLight = Color(0xFF1E1E1E); // Light backgrounds
  static const Color surfaceWhite = Color(0xFFFFFFFF); // Pure white

  // Semantic colors
  static const Color error = Color(0xFFF12D2D); // Error states
  static const Color success = Color(0xFF50CD89); // Success states
  static const Color warning = Colors.orange; // Warning states

  // Shimmer effect
  static const Color shimmerBase = Color(0x1A98C3FA);
  static const Color shimmerHighlight = Color(0xFFD2EDFB);

  // Surface and background
  static const Color surface = Color(0xFFFCFCFC); // Dark background
  static const Color surfaceContainer = Color(0xFFF0F0F0);
  static const Color background = Color(0xFFFFFFFF);


  // Text and icons
  static const Color onPrimary =  Color(0xFFFFFFFF);
  static const Color onSecondary =  Color(0xFFFFFFFF);
  static const Color onSurface = textPrimary;
  static const Color onSurfaceVariant = surfaceLight;
  static const Color onSurfaceSecondary = textDisabled;

  // Borders and shadows
  static const Color outline = border;
  static const Color shadow = Color(0x1A000000);
}

// Dark theme color scheme
class DarkThemeColors {
  // Primary brand colors
  static const Color primarySet1 = Color(0xFFEB7243); // Vibrant blue
  static const Color primarySet2 = Color(0xFFA6444B); // Vibrant blue
  static const Color primarySet3 = Color(0xFF422E49); // Vibrant blue



  static const Color primary = Color(0xFFA6444B); // Vibrant blue
  static const Color primaryContainer = Color(0x1A3E97FF); // Primary with opacity for containers

  // Secondary brand colors
  static const Color secondary = Color(0xFF232222); // Teal
  static const Color buttonColor = Color(0xFF232222); // Teal
  static const Color secondaryContainer = Color(0x1A18826E); // Secondary with opacity

  // Neutral colors (for text, borders, backgrounds)
  static const Color textPrimary = Color(0xFF2E2E2E); // Primary text and icons
  static const Color clickableText = Color(0xFF0067AC); // Primary text and icons
  static const Color textSecondary = Color(0xFF4B4B4B); // Secondary text
  static const Color textSecondaryAlpha = Color(0xFF4A4A4A); // Secondary text
  static const Color textTertiary = Color(0xFF1A1A1A); // Subtle text, hints
  static const Color textDisabled = Color(0xFF6E6E6E); // Disabled or low-emphasis text
  static const Color border = Color(0x14101010); // Borders, dividers
  static const Color surfaceLight = Color(0xFF1E1E1E); // Light backgrounds
  static const Color surfaceWhite = Color(0xFFFFFFFF); // Pure white

  // Semantic colors
  static const Color error = Color(0xFFF12D2D); // Error states
  static const Color success = Color(0xFF50CD89); // Success states
  static const Color warning = Colors.orange; // Warning states

  // Shimmer effect
  static const Color shimmerBase = Color(0x1A98C3FA);
  static const Color shimmerHighlight = Color(0xFFD2EDFB);

  // Surface and background
  static const Color surface = Color(0xFFFCFCFC); // Dark background
  static const Color surfaceContainer = Color(0xFFF0F0F0);
  static const Color background = Color(0xFFFFFFFF);

  // Text and icons
  static const Color onPrimary =  Color(0xFFFFFFFF);
  static const Color onSecondary =  Color(0xFFFFFFFF);
  static const Color onSurface = textPrimary;
  static const Color onSurfaceVariant = surfaceLight;
  static const Color onSurfaceSecondary = textDisabled;

  // Borders and shadows
  static const Color outline = border;
  static const Color shadow = Color(0x1A000000);
}